const express = require('express');
const fetch = require('node-fetch')
const router = express.Router();


router.get('/', function(req, res, next) {
//need to get data and send to render
});


module.exports = router;
